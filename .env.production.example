# ============================================
# PRODUCTION ENVIRONMENT
# ============================================
# Production configuration
# Port: 4567 (accessible from server IP)
# Services: PostgreSQL and Redis internal only

# Environment
NODE_ENV=production
PORT=4567
HOST=0.0.0.0

# Database (INTERNAL ONLY - not exposed)
DATABASE_URL=postgresql://music_admin:CHANGE_ME_STRONG_PASSWORD@postgres:5432/music_server?schema=public
POSTGRES_USER=music_admin
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=music_server

# Redis (INTERNAL ONLY - not exposed)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT - MUST BE CHANGED IN PRODUCTION
# Generate strong secrets: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_64
JWT_EXPIRATION=24h
JWT_REFRESH_SECRET=CHANGE_ME_USE_OPENSSL_RAND_BASE64_64
JWT_REFRESH_EXPIRATION=7d

# CORS - Configure with your frontend domain
# Example: CORS_ORIGINS=https://music.yourdomain.com,https://app.yourdomain.com
CORS_ORIGINS=*

# Music Library
MUSIC_LIBRARY_PATH=/music

# Cache
ENABLE_CACHE=true
CACHE_ALBUM_TTL=3600
CACHE_TRACK_TTL=3600
CACHE_ARTIST_TTL=7200

# File Storage
UPLOAD_PATH=/app/uploads/music
COVERS_PATH=/app/uploads/covers

# Bcrypt - Higher for production
BCRYPT_ROUNDS=12

# ============================================
# PRODUCTION SECURITY CHECKLIST
# ============================================
# ⚠️  BEFORE DEPLOYING:
# [ ] Change ALL passwords above
# [ ] Generate new JWT_SECRET and JWT_REFRESH_SECRET
# [ ] Configure CORS_ORIGINS with your actual frontend domain
# [ ] Review firewall rules (allow port 4567)
# [ ] Set up SSL/TLS with reverse proxy (nginx recommended)
# [ ] Configure backups for Docker volumes
# [ ] Set up monitoring and logging
