# ============================================
# Echo Music Server - Configuration
# ============================================
# Minimal configuration needed.
# Most settings have sensible defaults or are auto-configured.

# Environment (development or production)
NODE_ENV=development
PORT=3000

# Database (PostgreSQL)
DATABASE_URL=postgresql://music_user:music_password@localhost:5432/music_db

# Redis (for caching and job queues)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ============================================
# Optional (usually not needed)
# ============================================

# Data storage path (default: /app/data in Docker)
# DATA_PATH=/app/data

# Override auto-detected CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://192.168.1.100:3000

# JWT secrets are auto-generated on first run
# Only set these if you need to share secrets across multiple instances
# JWT_SECRET=your-jwt-secret-min-32-chars
# JWT_REFRESH_SECRET=your-jwt-refresh-secret

# Log level (auto-detected from NODE_ENV: debug in dev, info in prod)
# LOG_LEVEL=info
