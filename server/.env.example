# ============================================
# Echo Server - Development Configuration
# ============================================
# Generate automatically with: pnpm quickstart
# Or copy this file to .env and adjust values

# Core Settings
NODE_ENV=development
PORT=3000

# Database (docker-compose.dev.yml provides this)
DATABASE_URL=postgresql://music_user:music_password@localhost:5432/music_db

# JWT Secrets (auto-generated in Docker production)
# In development, these are optional (uses defaults)
JWT_SECRET=dev-secret-change-in-production-min-32-chars
JWT_REFRESH_SECRET=dev-refresh-secret-change-in-production

# Redis (docker-compose.dev.yml provides this)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=dev_redis_password

# ============================================
# The following are OPTIONAL with sensible defaults:
# ============================================
# DATA_PATH=./data               # Where to store data files
# LOG_LEVEL=debug                # Logging level (auto-detects from NODE_ENV)
#
# All other settings (library path, cache TTL, CORS, etc.)
# are configured through the admin interface or have
# sensible defaults that work out of the box.
