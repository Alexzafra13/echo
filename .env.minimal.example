# ============================================
# Echo Music Server - Minimal Configuration
# ============================================
#
# Quick Start (3 steps):
# 1. Copy this file: cp .env.minimal.example .env
# 2. Change MUSIC_PATH to where YOUR music is stored
# 3. Run: docker compose -f docker-compose.ghcr.yml up -d
#
# That's it! Everything else has sensible defaults.
# ============================================

# 🎵 WHERE IS YOUR MUSIC?
# Change this to point to your music folder on the host machine
MUSIC_PATH=/mnt/navidrome/musica

# 🔒 SECURITY (Auto-generated secure passwords)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=$(openssl rand -base64 32)
REDIS_PASSWORD=$(openssl rand -base64 32)

# 🌐 PORT (Optional - default is 4567)
# APP_PORT=4567

# ✅ DONE! Run: docker compose -f docker-compose.ghcr.yml up -d
# Then open: http://YOUR_SERVER_IP:4567
# Login: admin / admin123 (change password on first login)
