name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Tests & Build
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: music_admin
          POSTGRES_PASSWORD: music_password
          POSTGRES_DB: music_server
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate migrations are registered
        run: ./api/scripts/check-migrations.sh

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.18.3
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - name: Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('api/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        working-directory: api
        run: pnpm install --frozen-lockfile

      - name: Create test databases
        env:
          PGPASSWORD: music_password
        run: |
          for i in {1..4}; do
            psql -h localhost -U music_admin -d postgres -c "CREATE DATABASE music_server_test_$i;"
          done

      - name: Apply database schema
        working-directory: api
        env:
          DATABASE_URL: postgresql://music_admin:music_password@localhost:5432/music_server
        run: pnpm db:push --force

      - name: Apply schema to test DBs
        working-directory: api
        run: |
          for i in {1..4}; do
            echo "Applying schema to music_server_test_$i..."
            env DATABASE_URL="postgresql://music_admin:music_password@localhost:5432/music_server_test_$i" pnpm db:push --force
          done

      # Unit tests: Fast, use mocks, NO DB connection needed
      - name: Run unit tests
        working-directory: api
        run: pnpm test:unit

      # Integration tests: Test real DB/Redis operations
      - name: Run integration tests
        working-directory: api
        env:
          DATABASE_URL: postgresql://music_admin:music_password@localhost:5432/music_server_test_1
          JWT_SECRET: test-jwt-secret-for-ci-pipeline-min-32-chars
          JWT_REFRESH_SECRET: test-jwt-refresh-secret-for-ci-min-32
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          DATA_PATH: ${{ github.workspace }}/api/test-data
          MUSIC_LIBRARY_PATH: ${{ github.workspace }}/api/test-music
        run: pnpm test:integration

      # E2E tests: Full HTTP requests with real app
      - name: Run E2E tests
        working-directory: api
        env:
          DATABASE_URL: postgresql://music_admin:music_password@localhost:5432/music_server_test_1
          JWT_SECRET: test-jwt-secret-for-ci-pipeline-min-32-chars
          JWT_REFRESH_SECRET: test-jwt-refresh-secret-for-ci-min-32
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          DATA_PATH: ${{ github.workspace }}/api/test-data
          MUSIC_LIBRARY_PATH: ${{ github.workspace }}/api/test-music
        run: pnpm test:e2e

      - name: Build project
        working-directory: api
        run: pnpm build