# ============================================
# Echo Music Server - Configuration
# ============================================
#
# QUICK START:
#   1. Copy this file to .env
#   2. Set MUSIC_PATH to your music folder
#   3. Run: docker compose up -d
#   4. Open http://localhost:4567
#   5. Complete the setup wizard (create admin, configure library)
#
# First run will show a setup wizard to:
# - Create your admin account (no default password!)
# - Select your music library folder
# - Configure storage settings
#
# ============================================

# ============================================
# 🎵 Music Library Configuration (REQUIRED)
# ============================================
# Path to your music collection on the HOST machine
#
# Examples:
#   MUSIC_PATH=/mnt/music                    # Linux (NAS mount point)
#   MUSIC_PATH=/home/user/Music              # Linux (home folder)
#   MUSIC_PATH=C:/Users/YourName/Music       # Windows
#   MUSIC_PATH=/Users/YourName/Music         # macOS
#
MUSIC_PATH=/path/to/your/music

# ============================================
# 🌐 Application Settings
# ============================================
# Port where Echo will be accessible
# Default: 4567
# APP_PORT=4567

# Node environment (development/production)
# Default: production (when using Docker)
# NODE_ENV=production

# ============================================
# 🌍 Internet Access (HTTPS/SSL)
# ============================================
# Only needed if using docker-compose.production.yml
#
# Your domain name
# DOMAIN=music.yourdomain.com

# Email for SSL certificate (Let's Encrypt)
# SSL_EMAIL=your@email.com

# ============================================
# 🔒 Security Configuration
# ============================================
# IMPORTANT: In production, use strong passwords!

# Database password (PostgreSQL)
# Generate with: openssl rand -base64 32
# POSTGRES_PASSWORD=change_me_in_production

# Redis/Cache password
# Generate with: openssl rand -base64 32
# REDIS_PASSWORD=change_me_in_production

# JWT Secrets (Auto-generated if not set - Recommended!)
# Only set these if you need custom secrets
# Generate with: openssl rand -base64 64
# JWT_SECRET=
# JWT_REFRESH_SECRET=

# JWT Token Expiration
# Default: 7d (7 days)
# JWT_EXPIRATION=7d

# JWT Refresh Token Expiration
# Default: 30d (30 days)
# JWT_REFRESH_EXPIRATION=30d

# Bcrypt rounds for password hashing
# Range: 10-14 (higher = more secure but slower)
# Default: 12
# BCRYPT_ROUNDS=12

# ============================================
# 🌍 CORS (Cross-Origin Resource Sharing)
# ============================================
# Usually auto-configured. Only set if:
# - You have a custom domain (HTTPS)
# - You need specific origins
#
# Examples:
#   CORS_ORIGINS=https://music.yourdomain.com
#   CORS_ORIGINS=http://localhost:4567,https://music.example.com
#
# CORS_ORIGINS=

# ============================================
# 💾 Storage Configuration
# ============================================
# All data is stored in /app/data volume (Navidrome-style):
#   /app/data/secrets.env    - Auto-generated JWT secrets
#   /app/data/setup.json     - Setup wizard state
#   /app/data/metadata/      - Artist/album images
#   /app/data/covers/        - Album covers
#   /app/data/uploads/       - User uploads
#   /app/data/logs/          - Application logs
#
# These paths are inside the container - you don't need to change them
# DATA_PATH=/app/data
# UPLOAD_PATH=/app/data/uploads
# COVERS_PATH=/app/data/covers

# ============================================
# 📦 Cache Configuration
# ============================================
# Enable/disable Redis caching
# Default: true
# ENABLE_CACHE=true

# Cache TTL (Time To Live) in seconds
# Album cache duration (default: 3600 = 1 hour)
# CACHE_ALBUM_TTL=3600

# Track cache duration (default: 3600 = 1 hour)
# CACHE_TRACK_TTL=3600

# Artist cache duration (default: 7200 = 2 hours)
# CACHE_ARTIST_TTL=7200

# ============================================
# 🔧 Advanced Settings (Usually not needed)
# ============================================

# PostgreSQL Database Settings
# Only change if using external database
# POSTGRES_USER=music_admin
# POSTGRES_DB=music_server
# DATABASE_URL=postgresql://music_admin:password@postgres:5432/music_server

# Redis Settings
# Only change if using external Redis
# REDIS_HOST=redis
# REDIS_PORT=6379

# Application Host
# Default: 0.0.0.0 (listen on all interfaces)
# HOST=0.0.0.0

# API Prefix
# Default: api (URLs will be /api/albums, /api/tracks, etc.)
# API_PREFIX=api

# ============================================
# 📝 Build Metadata (Set by CI/CD)
# ============================================
# VERSION=1.0.0
# BUILD_DATE=2024-01-01
# VCS_REF=abc123
